# REST port of the Embedded / IoT Mentor. Needs Python 3.10+.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
# Only needed for POST /chat, which calls Claude through OpenRouter's
# OpenAI-compatible endpoint. Absent OPENROUTER_API_KEY disables that route
# without failing startup, so this stays a hard requirement rather than optional.
openai>=1.0.0