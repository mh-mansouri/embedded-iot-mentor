.claude/settings.local.json
__pycache__/
*.pyc

# Build output and dependencies of the VS Code extension scaffold.
node_modules/
vscode-extension/dist/
*.vsix

# The ChatGPT upload set is assembled from the skill folder on demand.
# Rebuild: python chatgpt-app/build_chatgpt_bundle.py
chatgpt-app/dist/
.venv/

# embedded-iot-mentor.skill lives at the repo root and is committed on purpose,
# so it can be downloaded and installed directly. Rebuild: python package_skill.py
