.claude/settings.local.json
__pycache__/
*.pyc

# embedded-iot-mentor.skill lives at the repo root and is committed on purpose,
# so it can be downloaded and installed directly. Rebuild: python package_skill.py
