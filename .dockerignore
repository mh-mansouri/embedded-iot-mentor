# The build context is the repository root, for both images. Everything here is
# either rebuilt inside the image or has no business in it.
.git/
.github/
.venv/
__pycache__/
*.pyc
.claude/
.playwright-mcp/

# Ports that no image needs.
vscode-extension/
vscode-copilot/
chatgpt-app/dist/

# Media only. The 2 MB demo gif is the expensive one; the reference .md files
# are the product and must stay.
embedded-iot-mentor-demo.gif
